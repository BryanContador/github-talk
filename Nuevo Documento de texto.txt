git --local 

git --system (a toda la compu)

git --global (a todos los repositorios de la pc)

git congif --global user.name "nombre" (set up username)

git congif --global user.email "gmail" (set up email)

git config --list (datos del usuario)

git config --global color.ui true

git config --global core.editor "code -wait" (set up visual studio as default)

git log (el historial del repo)         git log --oneline (en una linea)



##
cd - change directory
cd .. go back
ls  lista de carpetas
ls -a ver archivos ocultos
pwd ver ruta actual
mkdir nombre    crear carpetas
rmdir nombre    borrar carpeta

touch ("nombre.archivo" para crear un archivo. "hola.txt")
rm archivonombre (remove archivo.)

nano nombrearchivo (abrir en terminal) 
crl + o 





git branch   (ver ramas)
git branch nombre-rama (crear rama nueva)
git branch -d nombre-rama (delete la rama)
git switch -c nombre-rama (crear una rama y pasarse a ella)
git merge nombre-rama (estando dentro de la rama que se quiere jalar los cambios, se da el nombre de la rama donde están los cambios)



en .gitignore
el asterisco * seguido de la extensión (ej. *.jpg   *.txt) ignora todos los archivos con esa extensión
y si se pone el nombre completo no aparece. (ignora.py) dentro del .gitignore